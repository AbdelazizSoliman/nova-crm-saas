Invoice <%= @invoice.number %>

<%= @message_body.presence || "Please find attached your invoice." %>

Invoice summary:
- Client: <%= @client&.name || 'Client' %>
- Total amount: <%= number_to_currency(@invoice.total, unit: @invoice.currency || @account&.default_currency || 'USD') %>
- Status: <%= @invoice.status.to_s.titleize %>
- Issue date: <%= @invoice.issue_date || '-' %>
- Due date: <%= @invoice.due_date || '-' %>

The PDF copy of invoice <%= @invoice.number %> is attached to this email.

Regards,
<%= @account&.company_name || 'The Team' %>
